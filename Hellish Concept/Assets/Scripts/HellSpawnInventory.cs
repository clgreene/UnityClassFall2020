using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HellSpawnInventory : MonoBehaviour
{
    public bool starter;

    public bool Hellhound;
    public bool Ghoul;
    public bool Imp;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
